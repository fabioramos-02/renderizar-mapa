# Copie para .env e preencha. O .env é ignorado pelo git.
#   cp .env.example .env

# ==== CMS API (caminho padrão — scripts/gerar_relatorio.py) ====
# Api-Key pública read-only. Pegar no DevTools > Network >
# request /api/cms/ > header "Authorization: Api-Key <valor>".
CMS_API_BASE=https://admin.ms.gov.br/api/cms
CMS_API_KEY=

# ==== Banco (caminho DB — só na máquina com VPN) ====
DB_HOST=s0845.ms
DB_PORT=5432
DB_USER=
DB_PASSWORD=
DB_NAME=admin_prd
